{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "jest",
    "prepare": "husky",
    "build": "rm -rf ./.parcel-cache && npm run build --workspaces"
  },
  "devDependencies": {
    "@parcel/packager-ts": "^2.12.0",
    "@parcel/transformer-typescript-types": "^2.12.0",
    "husky": "^9.1.4",
    "jest": "^29.7.0",
    "lint-staged": "^15.2.9",
    "process": "^0.11.10",
    "typescript": "^5.5.4"
  },
  "lint-staged": {
    "src/**/*.ts": [
      "npm run format --workspaces",
      "npm run lint --workspaces"
    ],
    "src/**/*.tsx": [
      "npm run format --workspaces",
      "npm run lint --workspaces"
    ]
  }
}
